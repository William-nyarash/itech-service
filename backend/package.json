{
  "name": "itech",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "waren",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "build": "mkdir dist && cp -r src/*.js dist/",
    "build:ui": "rm -rf dist && npm run build && cp -r dist ."
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "dev": "nodemon src/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "body-parser": "^2.2.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "mongoose": "^8.18.0",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
